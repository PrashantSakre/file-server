import { addFile, files, getFileById } from "./File";
import { addAdmin, addUser, getAdminUsers, getUserById, users, getUserByName } from "./User";

export {
  users,
  addUser,
  getUserById,
  getUserByName,
  addFile,
  files,
  getFileById,
  getAdminUsers,
  addAdmin,
};
